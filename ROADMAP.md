# 产品组合器 · 接下来的计划

> 日期：2026-06-02
> 范围：v1 收尾 → 内测 → v1.5 规划
> 状态：M1–M4 代码已完成，待用户验收 + bug 修复

---

## 一、当前完成度盘点

### ✅ 已交付（代码 + commit + push）

| 里程碑 | 内容 | 交付物 |
|---|---|---|
| **M1** 脚手架 | Fastify+TS 后端 + Svelte5+TS 前端 + pnpm workspace | 6 个 API 端点 + 2 个页面 |
| **M2** Query | 4 Tab 双栏视图（品类/品牌/模块/未起量）+ 模块详情联动 | Query.svelte 重构 |
| **M3** 组合器 ⭐ | 5 维度解析 + 6 类模块匹配 + 资产对照 + 概念卡草案；象限改用户自选 | `/api/compose` + Compose.svelte |
| **M4** 洞察 | 品类×品牌矩阵 + 未起量 SKU 诊断（数据对比，不评判）| 2 个 API + CategoryMap.svelte + Underperform.svelte |

### 🟡 部分完成

| 项 | 状态 | 说明 |
|---|---|---|
| 数据底座 | 90% | 4 张 CSV 已就位，JOIN 命中率 63.4%（还差 130 条模糊匹配） |
| 概念卡 | 草案生成 OK，**保存未做** | M5 的核心任务 |
| Cockpit | 总览数字 OK，**TopN 板块未做** | M5 顺手 |

### ❌ 完全未做（v1 范围内）

- 概念卡持久化（无 `data/cards/*.json`）
- 概念卡列表页 `/concepts`
- Cockpit 加 TopN（本月新增模块 / 引用最多 / 未起量警告）
- 真实 LLM 接入 parseIntent（当前是关键词字典）
- 外部素材库 `10_external_materials.csv` 数据收集
- 运营入口 `/ops-gate`（v1.5 推迟）

---

## 二、立即处理（P0，阻塞）

### 🔴 1. 组合器「点击无反应」bug
**现象**：用户在 `/compose` 页面输入文本，点「开始组合 →」无反应。
**已确认**：
- 后端 POST `/api/compose` 状态 200，返回正常 JSON ✓
- typecheck 全过 ✓

**待用户排查**：
- F12 Network 看是否有 POST 请求
- vite 是否真的跑起来了，端口是否 5173
- F12 Console 是否有红字

**下一步**：拿到排查结果后我立即定位修复（10 分钟以内）。

---

## 三、v1 收尾（P1，1 周内）

按重要度排序：

### M5 · 概念卡保存 + Cockpit 升级（1-2 天）

**后端：**
- `POST /api/cards` 保存概念卡（写 `data/cards/{id}.json`）
- `GET /api/cards` 列出所有概念卡（含筛选：作者/品牌/品类/象限）
- `GET /api/cards/:id` 单卡详情
- `PATCH /api/cards/:id` 编辑（改象限/状态/名字）
- `DELETE /api/cards/:id`

**前端：**
- Compose.svelte 的「💾 保存概念卡」按钮启用
  - 保存时带：parsedIntent / matchedModules / userQuadrant / 创建时间 / 作者（v1 默认"内部用户"）
- 新页面 `/concepts` 概念卡列表
  - 卡片网格 + 筛选条
  - 点击进单卡详情
- Cockpit 加 3 个新板块：
  - 本月新增概念卡 N 张
  - 模块引用 TOP 5（哪个模块被组合最多）
  - 未起量警告（数量 + 点击跳 `/underperform`）

### 字典扩展 + 模糊匹配（半天）

- `composer.ts` 关键词字典补充（用户反馈"保暖"不识别等遗漏词）
- 加入同义词映射：保暖→保暖+暖和+加绒 等
- 模块 JOIN 改进：把 130 条 JOIN 失败的复用记录，用「产品名包含」二次模糊匹配

### 数据补全（半天，依赖业务方）

| 字段 | 来源 | 价值 |
|---|---|---|
| 上架时间 | 销售系统/ERP 导出 | 区分"新品冷启动 vs 长期卖不动" |
| 平台/曝光/转化 | 抖音/天猫店铺数据 | 区分"流量问题 vs 产品问题" |
| 评价/退货原因 | 客服系统 | 痛点反推 |

→ 党总指定一个人对接这块数据导出。

---

## 四、内测（P2，2 周）

### 准备工作（1 天）

- 部署：本地或内网部署一份
  - `pnpm build` 出生产构建
  - 后端 PM2 / 前端静态文件 nginx
  - 内网域名（如 `composer.taomarte.local`）
- 写一份《产品部内测使用指南》（1 页）
- 准备 5 个"种子查询"放在组合器输入框 placeholder 里

### 内测节奏

| 周 | 重点 | 产出 |
|---|---|---|
| W1 | 4 人各自试用 30 分钟/天 | 操作录屏 + 一句话反馈 |
| W2 | 党总主持 30 分钟例会复盘 | 调整字典 + 阈值 + 文案 |

### 衡量指标（成功定义）

> 内测 2 周后，至少 1 件**事实**成立：
>
> - 至少有 3 个产品概念卡进入打样讨论；
> - 至少有 5 次"我之前不知道这个模块能用在这"的发现；
> - 党总主动来用 ≥ 3 次/周。
>
> 任何一件不成立 = 项目假设有问题，停下来重审，不强推。

---

## 五、v1.5 规划（P3，2 周后启动）

按朱浩反馈 + 4 象限改用户自选后的方向。

### 运营入口 OpsGate（3 天）

- 新页面 `/ops-gate`
- 表单：提案描述 + 至少 1 类证据（销售数据/竞品 URL/客诉/市场趋势）
- 证据完整性校验（4 类至少 1 类）
- 进入"产品部待审区"列表
- 党总点单条 → 选「通过 / 退回补料 / 退回运营动作 / 合并到已有迭代」

### 真实 LLM 接入（2 天）

- 替换 `composer.ts` 里 `parseIntent()` 为调 openclaw HTTP 接口
- 保留规则字典作为 fallback（LLM 超时/失败时降级）
- 加 prompt 模板文件 `backend/src/prompts/parse_intent.md`
- 加 5 条回归测试样本（避免 LLM 换版本结果飘）

### 货盘自动解构（2 天）

- 上传 SKU 链接或图片 → 反向拆出"用了哪些模块"
- 入口：`/dissect` 单独页面
- 输入框 + 拖拽上传

### 外部素材库 v0（半天，业务侧填）

| 类 | 责任人 | 目标条数 |
|---|---|---|
| 材料厂样卡 | 那日苏 | 20 |
| 工艺包 | 供应链 | 10 |
| 行业资料 | 党英轩 | 10 |
| 跨品类灵感 | 全员 | 10 |

合计 50 条，作为「自由畅想」素材来源。

---

## 六、长期问题清单（不入 v1）

| 问题 | 影响 | 处理时机 |
|---|---|---|
| DISPIMG 图片只有 ID 没图本身 | 模块卡显示不出图，党总看着抽象 | v2 批量导出 OSS |
| 130 条 JOIN 失败的复用记录 | 部分模块"用在哪些 SKU"查不全 | v1.5 模糊匹配优化 |
| 23 个孤儿模块（库里有没用过） | 库里冗余 | 每季度 review 决定归档 |
| 4 象限阈值仍是模糊概念 | 用户自选时缺少参考 | 在每个象限按钮下加"提示阈值"（如 SKU ≥ 50） |
| 概念卡到打样的流程脱节 | 卡只存数字资产，没人推进 | v2 加打样工单生成 |

---

## 七、可执行的下一步（按时间排）

| 时间 | 动作 | 责任 |
|---|---|---|
| **今天** | 修组合器点击无反应 bug | Claude + 用户排查反馈 |
| 明天 | M5 后端：cards CRUD | Claude |
| 后天 | M5 前端：保存按钮 + /concepts 列表 + Cockpit 升级 | Claude |
| 第 4-5 天 | 字典扩展 + 模糊匹配优化 | Claude |
| 第 6 天 | 出 v1 发布版 + 写内测指南 | Claude + 用户 |
| 第 7 天 | 4 人内测开始 | 党总、曹、黄、那 |
| 第 14 天 | 内测复盘 | 党总主持 |
| 第 15+ 天 | 启动 v1.5（OpsGate / LLM 接入 / 货盘解构） | Claude |

---

## 八、需要业务侧拍板的问题

1. **概念卡作者标签**：v1 是默认"内部用户"，还是要做飞书 OAuth 登录？
   - 建议：v1 先做简单的"前端选名字"，v1.5 接 SSO
2. **概念卡保存位置**：`data/cards/*.json` 入 git 还是 gitignore？
   - 建议：入 git，方便 4 人协作看
3. **未起量阈值**：当前用 0，要不要改为「同品类销量 P25 以下」自适应？
   - 建议：加一个滑条让党总自己调
4. **上架时间数据**：谁负责导出？
   - 建议：党总指定一个责任人，3 个工作日内给出
5. **真实 LLM**：是接 openclaw / Kimi 直连 / 还是 Claude？
   - 建议：先 openclaw（已有）→ 不够好再换

---

## 九、风险与应对

| 风险 | 概率 | 影响 | 应对 |
|---|---|---|---|
| 4 人内测嫌烦不用 | 中 | 高 | 提前 1 周让他们参与 UI review，让他们有"主人翁"感 |
| LLM 接入后输出不稳 | 高 | 中 | 保留规则字典 fallback；加 5 条回归测试 |
| 上架时间数据拖延 | 高 | 中 | 未起量诊断先用"销量百分位"代替，能跑就跑 |
| openclaw 服务挂掉 | 低 | 中 | 加健康检查 + 失败优雅降级 |
| 党总临时变方向 | 中 | 高 | 文档化每次决策点，便于回溯 |

---

## 十、一句话总结

> v1 已经写完 80% 的代码，剩下 20%（M5 + 修 bug）2 天内完成；
> 然后 4 人内测 2 周，**用真实使用数据决定 v1.5 做什么**。
> 不要在 v1 没人用之前就堆 v1.5 功能。
